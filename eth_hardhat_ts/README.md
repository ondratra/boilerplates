# Ethereum + Hardhat + Solidity + TypeScript boilerplate

## Install

```
yarn
```

## Use

```
yarn build
yarn test

yarn checks # check format and lint
yarn checks:lint:fix # fix format and lint issues

yarn clean # remove all build artifacts
```
